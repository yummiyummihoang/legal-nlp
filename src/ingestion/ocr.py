"""Xử lý OCR cho các tài liệu dạng scan/ảnh."""
from pathlib import Path


def perform_ocr(image_or_pdf_path: Path) -> str:
    """Chạy OCR nhận dạng chữ tiếng Việt từ văn bản quét."""
    raise NotImplementedError
